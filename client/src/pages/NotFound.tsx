import Home from './Home';

/**
 * NotFound: Fallback component that renders Home
 * This is a workaround for GitHub Pages routing issues
 */
export default function NotFound() {
  return <Home />;
}
